---
---

<section class="hero">
  <div class="hero-inner">
    <p class="hero-label">Plateforme de vote électronique</p>
    <h1 class="hero-title">La démocratie dans votre organisation</h1>
    <p class="hero-subtitle">
      Votez.com est une solution de vote électronique développée par Neosapiens.
      Sécurisée, transparente et simple d'utilisation.
    </p>
    <div class="hero-actions">
      <a href="#contact" class="btn btn-primary">Demander une démonstration</a>
      <a href="#services" class="hero-link">Découvrir nos services <span aria-hidden="true">&rarr;</span></a>
    </div>
  </div>
</section>

<style>
  .hero {
    background-color: var(--color-navy);
    color: #fff;
    padding: 7rem 2rem 6.5rem;
    position: relative;
    overflow: hidden;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(37, 99, 235, 0.3), transparent);
  }

  .hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    position: relative;
    z-index: 1;
    max-width: 40rem;
  }

  .hero-label {
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 1.25rem;
  }

  .hero-title {
    font-size: 3.25rem;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #fff;
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.65);
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .hero-link {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.9375rem;
    font-weight: 500;
    transition: color var(--transition);
  }

  .hero-link:hover {
    color: #fff;
  }

  .hero-link span {
    display: inline-block;
    transition: transform var(--transition);
  }

  .hero-link:hover span {
    transform: translateX(3px);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 4.5rem 1.25rem 4rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .hero-actions {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.25rem;
    }
  }
</style>
