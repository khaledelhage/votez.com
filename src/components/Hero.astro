---
---

<section class="hero">
  <div class="hero-blob hero-blob-1"></div>
  <div class="hero-blob hero-blob-2"></div>
  <div class="hero-inner">
    <p class="hero-label">Votez.com</p>
    <h1 class="hero-title">La démocratie<br />dans votre organisation</h1>
    <p class="hero-subtitle">
      Une plateforme de vote électronique sécurisée, développée par Neosapiens.
      De la création de votre campagne à l'annonce des résultats, nous nous occupons de tout.
    </p>
    <div class="hero-actions">
      <a href="#contact" class="btn btn-primary btn-lg">Demander une démonstration</a>
    </div>
    <div class="hero-trust">
      <span class="hero-trust-item">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.3 4.7L6.3 11.7L2.7 8.1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Hébergé au Québec
      </span>
      <span class="hero-trust-item">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.3 4.7L6.3 11.7L2.7 8.1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Chiffrement avancé
      </span>
      <span class="hero-trust-item">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.3 4.7L6.3 11.7L2.7 8.1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        20+ ans d'expérience
      </span>
    </div>
  </div>
</section>

<div class="wave-divider">
  <svg viewBox="0 0 1200 60" preserveAspectRatio="none">
    <path d="M0,60 C200,20 400,50 600,30 C800,10 1000,45 1200,25 L1200,0 L0,0 Z" fill="var(--color-dark)"/>
  </svg>
</div>

<style>
  .hero {
    background: linear-gradient(170deg, var(--color-dark) 0%, var(--color-dark-lighter) 60%, #2D3D4F 100%);
    color: #fff;
    padding: 8rem 2rem 7rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero-blob {
    position: absolute;
    z-index: 0;
    animation: morph 12s ease-in-out infinite;
  }

  .hero-blob-1 {
    top: -10%;
    right: -10%;
    width: 50vw;
    height: 50vw;
    background: rgba(95, 159, 224, 0.07);
    border-radius: 40% 60% 60% 40% / 70% 30% 70% 30%;
  }

  .hero-blob-2 {
    bottom: -15%;
    left: -5%;
    width: 35vw;
    height: 35vw;
    background: rgba(94, 111, 127, 0.07);
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    animation-direction: reverse;
    animation-duration: 10s;
  }

  @keyframes morph {
    0%, 100% { border-radius: 40% 60% 60% 40% / 70% 30% 70% 30%; }
    33% { border-radius: 60% 40% 70% 30% / 40% 60% 30% 70%; }
    66% { border-radius: 30% 70% 40% 60% / 60% 40% 70% 30%; }
  }

  .hero-inner {
    max-width: 42rem;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-label {
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: rgba(255, 255, 255, 0.3);
    margin-bottom: 2rem;
    font-family: var(--font-body);
  }

  .hero-title {
    font-size: 3.75rem;
    font-weight: 400;
    line-height: 1.08;
    letter-spacing: -0.025em;
    color: #fff;
    margin-bottom: 1.75rem;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.7;
    max-width: 32rem;
    margin: 0 auto 2.5rem;
  }

  .hero-actions .btn-lg {
    padding: 1.0625rem 2.75rem;
    font-size: 1rem;
  }

  .hero-trust {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 3.5rem;
    padding-top: 3rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .hero-trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8125rem;
    color: rgba(255, 255, 255, 0.35);
    font-weight: 600;
  }

  .hero-trust-item svg {
    color: rgba(255, 255, 255, 0.25);
  }

  .wave-divider {
    width: 100%;
    overflow: hidden;
    line-height: 0;
    margin-top: -1px;
  }

  .wave-divider svg {
    display: block;
    width: 100%;
    height: 60px;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 1.25rem 4rem;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.0625rem;
    }

    .hero-trust {
      flex-direction: column;
      align-items: center;
      gap: 0.75rem;
    }

    .wave-divider svg {
      height: 30px;
    }
  }
</style>
