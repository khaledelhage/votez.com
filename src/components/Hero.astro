---
---

<section class="hero">
  <div class="hero-inner">
    <p class="hero-label">Plateforme de vote électronique</p>
    <h1 class="hero-title">La démocratie dans votre organisation</h1>
    <p class="hero-subtitle">
      Votez.com est une solution de vote électronique développée par Neosapiens.
      Sécurisée, transparente et simple d'utilisation.
    </p>
    <div class="hero-actions">
      <a href="#contact" class="btn btn-primary">Lancer votre campagne de vote</a>
      <a href="#services" class="btn btn-ghost">Découvrir nos services</a>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    color: #fff;
    padding: 7rem 2rem 6rem;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 60%;
    height: 200%;
    background: radial-gradient(ellipse, rgba(52, 188, 242, 0.15) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-label {
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-primary);
    margin-bottom: 1rem;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.03em;
    max-width: 38rem;
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.8s ease-out;
  }

  .hero-subtitle {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.7;
    max-width: 34rem;
    margin-bottom: 2.5rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn-ghost {
    color: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 0.875rem 2rem;
    border-radius: var(--radius);
    font-weight: 500;
    transition: all var(--transition);
  }

  .btn-ghost:hover {
    background-color: rgba(255, 255, 255, 0.1);
    color: #fff;
    border-color: rgba(255, 255, 255, 0.5);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(1.5rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 4rem 1.25rem 3.5rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1.05rem;
    }

    .hero-actions {
      flex-direction: column;
    }

    .hero-actions .btn,
    .hero-actions .btn-ghost {
      text-align: center;
    }
  }
</style>
